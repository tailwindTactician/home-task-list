 List<int> numbers = new List<int>();

        if (numbers.Count == 0) 
        {
            Console.WriteLine("Список пустой");
        }
        else
        {
            Console.WriteLine("Список не пустой");
        }